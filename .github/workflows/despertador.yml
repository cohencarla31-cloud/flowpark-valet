name: Despertador Streamlit

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Tocar la puerta de la app
        run: |
          curl -I -s -o /dev/null -w "%{http_code}" https://flowpark-valet.streamlit.app/
